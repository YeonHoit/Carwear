<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- 타이틀 바 삭제 -->
        <item name="windowNoTitle">true</item>
        <!-- 상태 바 투명 -->
        <item name="android:windowTranslucentStatus" tools:targetApi="kitkat">true</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EditTextTheme">
        <item name="colorControlActivated">@color/default_color</item>
        <item name="colorControlNormal">#666666</item>
        <item name="android:textSelectHandle">@color/default_color</item>
        <item name="android:textColorHighlight">@color/transparent_defalut_color</item>
        <item name="android:textCursorDrawable">@color/default_color</item>
    </style>

</resources>